<nav>
    <div class="nav-wrapper green">
      <a href="/user/home" class="brand-logo"><img src="../../images/BB_logo_wht.png" id="nav-logo" alt="" width="200" height="auto"></a>
      <ul id="nav-mobile" class="right hide-on-med-and-down">
        <li><a href="/calendar">History</a></li>
        <li>|</li>
        <li><a href="/user/home">About</a></li>
        <li>|</li>
        <li><a href="/user/sign-out">Log Out</a></li>
      </ul>
    </div>
</nav>


<main class="container">
    <div class="row light-green lighten-4" id="card-by-cat">

        <div class="col m2"></div>

        <div class="col s12 m8" id="order-look">
            <div class="card green">
                <div id="category-card" class="white-text">
                    {{!-- <span class="card-title">Orders</span> --}}
                    <h4 class="center-align">See Orders By Category</h4>
                    <div class="row center-align">
                        <div class="col s3"></div>
                        <div class="input-field col s6">
                            <p class="center-align">
                                {{!-- <select class="browser-default col s12 m6 l4" id = "search-item-category">
                                <option value="" disabled selected>Search by your category</option>
                                {{#each this.categories}}
                                    <option value="{{this.id}}">{{this.name}}</option>
                                {{/each}}
                                </select> --}}
                                <select class="browser-default" id = "search-item-category">
                                <option value="" disabled selected>Search by your category</option>
                                {{#each this.categories}}
                                    <option value="{{this.id}}">{{this.name}}</option>
                                {{/each}}
                                </select>
                            </p>
                           
                            <div id="items-table"></div>
                        </div>
                        <div class="col s3"></div>
                    </div>
                </div>
            </div>
        </div>
        
        <div class="col m2"></div>
    </div>





</main>


<script src="/js/order.js"></script>